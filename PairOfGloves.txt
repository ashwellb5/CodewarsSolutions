def number_of_pairs(gloves):
    colors = {}
    pairs = 0
    for x in gloves:
        if x not in colors.keys():
            colors[x] = 1
        else:
            colors[x] = colors[x] + 1
    for x in colors:
        pairs += colors[x] / 2 // 1
    return pairs